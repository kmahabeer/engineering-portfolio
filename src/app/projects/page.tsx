import { ProjectGallery } from '@/components';

const page = async () => {
	return (
		<div className='min-h-screen px-4 mx-auto lg:max-w-6xl'>
			<ProjectGallery />
		</div>
	);
};

export default page;
